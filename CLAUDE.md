# このプロジェクト
- storiff = story + diffの意味。
ブラウザ上でcritのことができるが、storyがありそのdiffを見れる。
- https://github.com/tomasz-tomczyk/crit
storyの粒度はcommitではなく、AIが考えたもの。

https://github.com/tomasz-tomczyk/crit をベースに、差分をストーリー表示できる
# やりたいこと
でかい差分をレビューするにもどこから見ればわからない。
どこから見たらいいかわからない。
そのため、ストーリー形式でAIに出してもらい、少しずつ全体が分かるようにしたい。
このツールを使うことでユーザーに、差分を把握・背景・ロジックを把握し、、他の人へ説明ができるほどの理解を与える
# 紙芝居形式
「まずこれを実装しました。
というのも~だからです」など説明分
その時の差分(これはコミットとは別で、AIがいい感じに、作業があれば思い出し、なければ想定したストーリー)
粒度は結構小さめ。まずこのファイルから行います。
で、途中でAIはあ、なんか変です。漏れがありますとか、自ら気づき、応答してくれる
- ストーリーがすべて合わされば、指定した差分のdiffになる

# 流れ
- /storiff を行うと、claudeは過去の実装のストーリーを思い出す/もしくは想定し、ストーリーを考える
- そのストーリーをもとに、差分を見せる
- その場で質問などcrit的にできる

# 検討事項
- ファイル表示順の制御(owns_files の並び)を残すか廃止するか
  - 右側ミニマップ(いま全体のどこを見ているか)は全ファイルを固定順で並べて表示する
  - story ごとにファイル順を変える機能があると、ミニマップのファイル配置が story ごとに一貫しなくなる懸念
  - ミニマップは差分の自然順(prep が出す順)で固定し、owns_files の順序制御は廃止する案を検討中。未決

# メモ
- push 前に .claude-plugin/plugin.json:4 の "version" を上げる
